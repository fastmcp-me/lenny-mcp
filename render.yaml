services:
  - type: web
    name: lenny-mcp
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: MCP_MODE
        value: sse
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
